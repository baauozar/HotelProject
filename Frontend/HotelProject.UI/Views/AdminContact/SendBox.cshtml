@model List<ResultSendBoxDto>
@{
    ViewData["Title"] = "SendBox";
    Layout = "~/Views/AdminLayout/_AdminLayout.cshtml";
}

<div class="email-left-box">
    <a href="/AdminContact/AddSendMessage/" class="btn btn-primary btn-block">Compose</a>
    @await Component.InvokeAsync("_SideBarAdminContactPartial")
    <h5 class="mt-5 m-b-10">Categories</h5>
    @await Html.PartialAsync("SideBarAdminContactCategoryPartial")
</div>
<div class="email-right-box">
    <div role="toolbar" class="toolbar">
        <div class="btn-group">
            <button aria-expanded="false" data-toggle="dropdown" class="btn btn-dark dropdown-toggle" type="button">
                Sned Messages <span class="caret m-l-5"></span>
            </button>

        </div>
    </div>
    <div class="email-list m-t-15">
        @foreach (var item in Model)
        {
            <div class="message">
                <a href="/AdminContact/MessageDetailsBySender/@item.SendMessageId">
                    <div class="col-mail col-mail-1">
                        <div class="email-checkbox">
                            <input type="checkbox" id="chk2">
                            <label class="toggle" for="chk2"></label>
                        </div>
                        <span class="star-toggle ti-star"></span>
                    </div>
                    <div class="col-mail col-mail-2" style="display: grid; grid-template-columns: 1fr auto; gap: 20px; align-items: center;">
                        <div class="subject"><b>Title:@item.Title</b> Receiver Name: @item.ReceiverName</div>
                        <div class="date">@item.Date.ToString("dd-MM-yyyy")</div>
                    </div>
                </a>
            </div>
        }
    </div>


</div>





@* @(item.Content?.Substring(0, Math.Min(item.Content.Length, 30)) ?? "")... 
 *@